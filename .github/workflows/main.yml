name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master_3.2.2 branch
  push:
    branches: [ master_3.2.2 ]
  pull_request:
    branches: [ master_3.2.2 ]
